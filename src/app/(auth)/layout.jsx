export default function Component({children}) {
    return (
        <>
            <h1>
               Common auth layout like siginup, login tab
            </h1>
            {children}
        </>
    );
}
